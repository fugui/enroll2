<view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 登录
    </button>    
    <view wx:else style="justify-content: center">
      <van-loading type="spinner" color="#1989fa" >加载中...</van-loading>
    </view>

  </view>

</view>
